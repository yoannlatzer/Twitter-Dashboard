(function($, block) {
block.fn.ads = function(config) {

//functie voor gem (avg(sum(ta uit heatmap.temperatures)))

//neem je regen mee in bepaling??

//return op wiskundige basis van gem en regen per dag een ad + hyperlink in html format
//prob returns getal? getal = img + link

 // <a href="getal_link">  <img src="getal_img"  alt="getal_alt">



    return this.$element;
};
})(jQuery, block);
